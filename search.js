var obj = { name: 'Neel' };
var data = [ { name: 'Neel', num: 213 }, { name: 'Gracie' } ];

var objKeys = Object.keys(obj);

data.forEach(function(topEl, index, array){
  var elKeys = Object.keys(topEl);
  var searchState = true;

  elKeys.forEach(function(el, index, array){
    if(topEl[el] === top){
      
    } 
  })
});